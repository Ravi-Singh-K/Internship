# # Prints the square of the given list elements
# numbers = [1,2,3,4,5,6,7]
# squared_numbers = [num*num for num in numbers]
# print(squared_numbers)

# # Prints the even numbers from the given list elements
# even_numbers = [num for num in numbers if num%2 == 0]
# print(even_numbers)

# Prints the compared character 
# text = "I Love Python"
# vowels = "aeiou"
# extracted_characters = [char for char in text if char in vowels]
# print(extracted_characters)


a = {'a':1, 'b':2, 'c':3}
print(a.keys())
# b = { x for x in a.keys()}
# print(b)

# type hint type annotation  memory reference | functions | map filter using lambda or without  | highest lowest with second also    
# print(id(b))